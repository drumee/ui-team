# @casualoffice/docs declares peer ranges that conflict with the @univerjs
# 0.25.1 set @casualoffice/sheets needs; npm 7+ refuses the tree (ERESOLVE)
# unless peers are resolved the npm 6 way. Every peer this project relies on
# is listed explicitly in package.json.
legacy-peer-deps=true
