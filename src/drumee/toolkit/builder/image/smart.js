
const __core = require("../builder");
const __skl_image_smart= function(props, style) {
  props = props || {};
  props.flow = _a.x;
  const x = new __core(props, style);
  const a = x.render({
    kind : KIND.image.smart
  });
  return a;
};

module.exports = __skl_image_smart;